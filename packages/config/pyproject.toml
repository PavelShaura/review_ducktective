[project]
name = "ducktective-config"
version = "0.1.0"
description = "Настройки приложений"
requires-python = ">=3.12"

dependencies = [
    "pydantic-settings>=2.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ducktective"]
