"""Доступ к языковым моделям.

LiteLLM при инициализации тянет справочник цен с raw.githubusercontent.com.
Для air-gapped профиля это недопустимо: заявлено, что наружу не уходит ни одного
запроса. Встроенной копии справочника достаточно — у локальных моделей стоимость
нулевая. Переменная выставляется до первого импорта litellm, поэтому живёт здесь,
а не в модуле клиента.
"""

import os


os.environ.setdefault("LITELLM_LOCAL_MODEL_COST_MAP", "True")
